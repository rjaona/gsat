# Candidats — Axe 4 Résilience opérationnelle & infra (fan-out lecture seule 2026-08-30)
Décompte : 4 BLOQUANT · 4 MAJEUR · 2 MINEUR.

- [BLOQUANT] `supabase/config.toml:357-358` (`[edge_runtime] enabled=false`) + `PASSATION_LOT1.md:69` — un cycle `supabase stop && supabase start` (fallback envisagé pour appliquer le SMTP) régénère la stack depuis ce fichier → edge non géré par la CLI (explique « edge tombé, RestartPolicy=no »). manage-user/chat-with-ai cassent. VÉRIF PROD : `docker inspect supabase_edge_runtime_gsat --format '{{.HostConfig.RestartPolicy.Name}} {{.State.Status}}'` + config.toml réel du VPS.
- [BLOQUANT] Drift bootstrap DB : `schema.sql`+`rls_policies.sql`+`hook_custom_access_token.sql`+`trigger_on_score_write.sql` HORS `supabase/migrations/` ; `config.toml:58` `migrations.schema_paths=[]`, `:65` seed `./seed.sql` inexistant → `supabase db reset` ne rejoue QUE migrations → base sans tables/RLS/hook/trigger. Repo NON reconstructible fidèlement. PROUVÉ REPO.
- [BLOQUANT] `gsat_backup_pre_v3_0_20260820-152547.sql` (1,3 Mo, racine, untracked mais PAS dans `.gitignore`) — contient `CREATE TABLE auth.users` + `encrypted_password` + `COPY auth.users` (données) + `COPY vault.secrets`. Risque `git add .` → push hash mots de passe vers GitHub ; + gotcha OneDrive /mnt/d. Seule preuve d'un « backup » = dumps ad hoc en clair, aucune stratégie auto. PROUVÉ REPO. **Correctif immédiat trivial : déplacer/supprimer/chiffrer + gitignore.**
- [BLOQUANT] SMTP réel non appliqué : `config.toml:219-227` `[auth.email.smtp]` commenté, `:100` inbucket enabled ; spec chantier D confirme GoTrue→inbucket + `API_EXTERNAL_URL=127.0.0.1`. Recovery mot de passe + notifs email morts (33 comptes). VÉRIF PROD : `docker exec supabase_auth_gsat printenv | grep -E 'SMTP|API_EXTERNAL_URL|MAILER'`.
- [MAJEUR] `config.toml:182` `[auth.rate_limit] email_sent=2` — limite GLOBALE projet 2 emails/h → rollout national : seuls 2 resets/h aboutissent, reste 429 silencieux. VÉRIF PROD valeur active.
- [MAJEUR] Aucune IaC versionnée (pas de docker-compose, aucune `restart:`/`unless-stopped` dans le repo) → orchestration prod hors revue ; reboot VPS = pas de garantie de remontée des conteneurs (db/auth/kong/storage). VÉRIF PROD restart policies.
- [MAJEUR] Aucun monitoring/alerting versionné ; 2 jobs `pg_cron` (close-expired-campagnes horaire, cloturer-evaluations-non-revues quotidien) sans supervision `cron.job_run_details` → échec silencieux détecté tard.
- [MAJEUR] Firewall hPanel Hostinger — dépendance fragile répétée dans les runbooks, bloque l'accès OPS (SSH root) depuis la dev par intermittence, aucune règle permanente → incident + firewall fermé = pas d'accès.
- [MINEUR] `config.toml:154-156` `site_url=127.0.0.1:3000` (+ mismatch http/https) → config versionné jamais aligné prod (config réelle modifiée à la main hors repo).
- [MINEUR] `supabase/edge-functions/chat-with-ai/index.ts:11` `MODEL='claude-3-5-sonnet-20241022'` codé en dur sans fallback (dépréciation → 502 générique).

POSITIF : CORS restreint (pas de wildcard) sur les 2 edge ; rate-limit applicatif chat 20/min ; frontend chantier D livré+TDD (blocage = ops App Password) ; `.env*`/`functions/.env` gitignorés ; pg_cron upsert idempotent par nom.
