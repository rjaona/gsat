# Candidats — Axe 2 Intégrité & robustesse données (fan-out lecture seule 2026-08-30)
Décompte : 2 BLOQUANT · 2 MAJEUR · 4 MINEUR · 3 POSITIF.

- [BLOQUANT] `supabase/trigger_on_score_write.sql:20-212` — refait `CREATE OR REPLACE fn_recalculate_scores` avec le CORPS D'AVRIL BUGGÉ (`COALESCE(es.note,0)`=N/A compté 0 ; aucun filtre mode/socle ; OSN `score_par_dimension='{}'` ; pas de filtre referentiel_version en consolidation). Selon l'ordre bootstrap documenté (CLAUDE.md:266-267) ce fichier s'applique APRÈS `migrations/*` → clobbe la version corrigée (`20260804_faritany.sql:300-479`). En mode socle (cycle 1) → tous scores Faritany+OSN biaisés bas + consolidation mélange far/v3_0. VÉRIF PROD : `SELECT pg_get_functiondef('fn_recalculate_scores'::regproc);` (présence `v_mode` + `FILTER (WHERE NOT (es.id IS NOT NULL AND es.note IS NULL))` = corrigée vivante).
- [BLOQUANT] `trigger_on_score_write.sql:218-223` vs migration — le trigger `on_score_write` n'est câblé QUE dans le fichier buggé ; `scripts/setup_base_gsat_faritany.sh` n'applique que le migration → soit scorer buggé, soit AUCUN trigger (scores jamais recalculés). VÉRIF PROD : `SELECT tgname FROM pg_trigger WHERE tgrelid='evaluation_scores'::regclass;`
- [MAJEUR] `src/services/referentielService.ts:224,241,256` (`updateReferentiel`) — `.upsert()` SANS `onConflict`, id absent du payload → INSERT viole `UNIQUE(version)/(ref_id,code)/(dimension_id,code)` → 23505 au 2e import UI. (Le script seed, lui, a onConflict → bootstrap far sûr.)
- [MAJEUR] `trigger_on_score_write.sql:206-211` (et migration) — `EXCEPTION WHEN OTHERS THEN RAISE WARNING…RETURN` avale toute erreur de recalcul → score périmé silencieux, aucune alerte. (Observabilité.)
- [MINEUR] `supabase/schema.sql:107` — commentaire `parent_version='3.0'` alors que data/migration = `'v3_0'` (TEXT non-FK) ; une insertion suivant le commentaire casserait le chaînage silencieusement.
- [MINEUR] `supabase/seeds/referentiel_v3_0_seed.sql:6+` — `ON CONFLICT DO UPDATE` critères omet `socle/source_codes/indicateur_erp` → re-seed correctif ne rafraîchit pas ces colonnes.
- [MINEUR] `supabase/rls_policies.sql:493-503` — `audit_log` RLS sans `FORCE ROW LEVEL SECURITY` → service_role/owner bypass RLS peut altérer/supprimer (append-only garanti clients seulement). VÉRIF PROD.
- [MINEUR] `CLAUDE.md:103-104` — doc désigne `referentielService.ts`+`trigger_on_score_write.sql` comme miroir scoring ; le VRAI miroir est `scoring.ts`+migration → risque de réintroduire le bug N/A. Test parité hors CI (PG :5433 requis).

POSITIF : parité `scoring.ts` ↔ `fn_recalculate_scores` corrigé prouvée sur 4 cas (absent=0, N/A exclu num+dén, dim tout-N/A=null, essentiel N/A≠KO, mode socle) ; modèle N/A/absent préservé par mappers (pas de `||0`) ; far⊂v3_0 = 88 codes source, 0 orphelin ; seeds campagne idempotents.
